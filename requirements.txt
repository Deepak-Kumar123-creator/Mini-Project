# =============================================================================
# requirements.txt — AI Healthcare Recommendation System
# =============================================================================
# Install all dependencies with:
#   pip install -r requirements.txt
# =============================================================================

# ---- Flask Web Framework ----
flask>=2.3.0              # Core Flask web framework
flask-cors>=4.0.0         # Cross-Origin Resource Sharing (CORS) support
flask-sqlalchemy>=3.0.0   # SQLAlchemy ORM integration for Flask

# ---- Machine Learning ----
scikit-learn>=1.3.0       # Random Forest Classifier, metrics, model selection
numpy>=1.24.0             # Numerical arrays (used by scikit-learn + predict.py)
pandas>=2.0.0             # DataFrame for loading/preprocessing the CSV dataset

# ---- Production Server ----
gunicorn>=21.0.0          # Production-grade WSGI server (used in Docker)

# ---- Serialization (model saving) ----
# pickle is part of Python stdlib — no extra install needed
